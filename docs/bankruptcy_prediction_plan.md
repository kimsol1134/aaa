# 🎯 부도예측 모델 개발 최종 실행 계획

## 📁 1단계: 프로젝트 구조
```
junwoo/
├── data/
│   ├── raw/                          # 원본 데이터
│   ├── processed/                    # 전처리된 데이터
│   └── features/                     # 엔지니어링된 특성
├── notebooks/
│   ├── 01_도메인_기반_부도원인_분석.ipynb
│   ├── 02_고급_도메인_특성공학.ipynb
│   ├── 03_상관관계_및_리스크_패턴_분석.ipynb
│   ├── 04_불균형_분류_모델링.ipynb
│   └── 05_모델_평가_및_해석.ipynb
├── src/
│   ├── domain_features/
│   │   ├── liquidity_crisis.py      # 유동성 위기 특성
│   │   ├── insolvency_patterns.py   # 지급불능 패턴
│   │   ├── korean_market.py         # 한국 시장 특화
│   │   └── manipulation_detection.py # 재무조작 탐지
│   ├── models/
│   ├── evaluation/
│   └── utils/
├── streamlit_app/
│   ├── app.py                       # 메인 대시보드
│   ├── pages/
│   │   ├── risk_monitor.py          # 실시간 모니터링
│   │   ├── early_warning.py         # 조기경보 시스템
│   │   └── scenario_analysis.py     # 시나리오 분석
│   └── components/
├── docs/
│   └── bankruptcy_prediction_plan.md # 이 계획서
└── configs/
```

## 📊 2단계: 도메인 기반 부도 원인 분석

### 핵심 질문: "왜 기업이 부도가 나는가?"

#### 2.1 부도의 근본 원인 (횡단면 데이터 기준)
- **유동성 위기**: 유동비율 < 1, 당좌비율 < 0.5
- **지급불능**: 부채비율 > 200%, 자기자본 < 0
- **현금흐름 문제**: 영업현금흐름 < 0, 현금비율 극저
- **수익성 악화**: 영업이익률 < 0, ROE < -10%
- **효율성 저하**: 총자산회전율 하위 10%

#### 2.2 한국 시장 특화 부도 패턴
- 대기업 의존도 리스크 (매출 집중도)
- 수출 비중과 환노출
- 부동산 자산 비중 과다
- 업종별 특성 (제조업 vs 서비스업)
- 외감기업 vs 비외감기업 차이

#### 2.3 업종별 부도 특성
- **제조업**: 재고자산회전율, 매출채권회전율
- **건설업**: 미수금 비율, 프로젝트 수주 잔고
- **유통업**: 재고 비중, 매출총이익률
- **IT/서비스**: 무형자산 비중, R&D 투자

## 🔧 3단계: 고급 도메인 특성 공학

### 3.1 유동성 위기 지표 (현재 시점)
```python
# 즉각적 지급능력
instant_liquidity = (현금 + 현금성자산) / 유동부채

# 운전자본 건전성
working_capital_health = (유동자산 - 유동부채) / 매출액

# 현금 소진 위험도
cash_burn_risk = 영업비용 / (현금 + 현금성자산)

# 긴급 자금조달 여력
emergency_funding = (미사용신용한도 + 유동성자산) / 단기차입금
```

### 3.2 재무 건전성 종합 지표
```python
# 자본 잠식도
capital_erosion = max(0, 1 - (자본총계 / 납입자본금))

# 차입금 의존도
debt_dependency = (단기차입금 + 장기차입금) / 자산총계

# 이자보상배율
interest_coverage = EBIT / 금융비용

# 부채 상환 능력
debt_service_coverage = (당기순이익 + 감가상각비) / 총차입금
```

### 3.3 재무조작 탐지 (Beneish M-Score 한국형)
```python
# 매출채권 이상 비율
receivables_abnormality = 매출채권 / 매출액

# 재고자산 이상 적체
inventory_abnormality = 재고자산 / 매출원가

# 발생액 품질
accruals_quality = (당기순이익 - 영업현금흐름) / 자산총계

# 비용 자본화 의심
expense_capitalization = 무형자산증가액 / 영업비용
```

### 3.4 이해관계자 신뢰도 지표
```python
# 신용등급 위험도
credit_rating_risk = 기업신용평가등급_점수화

# 연체 이력 점수
delinquency_score = sum(연체건수_30일 + 연체건수_60일*2 + 연체건수_90일*3)

# 공공정보 리스크
public_info_risk = 세금체납건수 + 압류건수 + 소송건수

# 감사 의견 리스크
audit_risk = 외감여부 * (감사의견_점수)
```

## 📈 4단계: 상관관계 및 리스크 패턴 분석

### 4.1 횡단면 리스크 패턴
- 부도기업 vs 정상기업 주요 차이점
- 업종별 부도 패턴 차이
- 규모별 부도 특성 (대/중/소기업)
- 지역별 부도 분포

### 4.2 변수 간 상호작용
- 레버리지 × 수익성
- 유동성 × 운전자본
- 규모 × 효율성
- 업종 × 재무비율

### 4.3 비선형 관계 포착
- 다항식 특성 (2차, 3차)
- 로그 변환 특성
- 비율의 비율 특성
- 분위수 기반 버킷팅

### 4.4 특성 중요도 분석
- Information Value (IV)
- Gini Importance
- Permutation Importance
- SHAP value 기반 중요도

## 🤖 5단계: 불균형 분류 모델링

### 5.1 SMOTE + Tomek Links 최적화
```python
# 소수 클래스 오버샘플링
smote = SMOTE(sampling_strategy=0.3, k_neighbors=5)

# 경계선 정리
smote_tomek = SMOTETomek(
    smote=smote,
    tomek=TomekLinks(sampling_strategy='majority')
)

# BorderlineSMOTE (경계선 샘플 중심)
borderline = BorderlineSMOTE(
    k_neighbors=5,
    m_neighbors=10,
    kind='borderline-1'
)
```

### 5.2 비용 민감 학습
```python
# 클래스 가중치
class_weights = {
    0: 1,     # 정상기업
    1: 20     # 부도기업 (20배 가중)
}

# Focal Loss (불균형 특화)
focal_loss = FocalLoss(alpha=0.25, gamma=2.0)

# 비즈니스 비용 기반
business_loss = lambda y_true, y_pred: (
    false_negative_cost * 50 +  # 부도 미탐지 비용
    false_positive_cost * 1      # 오탐지 비용
)
```

### 5.3 앙상블 전략
```python
# Tree-based 앙상블
models = {
    'lgb': LightGBM(is_unbalance=True),
    'xgb': XGBoost(scale_pos_weight=20),
    'cat': CatBoost(class_weights='Balanced')
}

# 불균형 특화 앙상블
balanced_models = {
    'balanced_rf': BalancedRandomForestClassifier(),
    'easy_ensemble': EasyEnsembleClassifier(),
    'rus_boost': RUSBoostClassifier()
}
```

## 📊 6단계: 평가 및 해석

### 6.1 평가 메트릭 (불균형 특화)
```python
# PR-AUC (핵심 지표)
pr_auc = average_precision_score(y_true, y_pred_proba)

# F2-Score (재현율 중시)
f2 = fbeta_score(y_true, y_pred, beta=2)

# Matthews Correlation Coefficient
mcc = matthews_corrcoef(y_true, y_pred)

# 비즈니스 메트릭
type2_error_rate = fn / (fn + tp)
expected_loss = calculate_business_loss(cm)
```

### 6.2 모델 해석
- **SHAP 분석**: Global/Local importance
- **LIME 분석**: 개별 예측 설명
- **Partial Dependence**: 변수별 영향도

## 🚀 7단계: Streamlit 배포 앱

### 7.1 메인 대시보드
```python
st.title("🚨 한국 기업 부도 위험 예측 시스템")

# 실시간 리스크 스코어
risk_score = model.predict_proba(features)[0, 1]

# 주요 위험 요인
plot_waterfall_chart(shap_values)

# 재무 건전성 지표
plot_financial_health_radar(metrics)

# 동종업계 비교
plot_peer_comparison(company, peers)

# 시나리오 분석
scenario_results = run_scenarios(data)

# AI 기반 추천
recommendations = generate_recommendations(factors)
```

### 7.2 API 엔드포인트
```python
@app.post("/api/predict")
async def predict_bankruptcy(company_data: dict):
    features = feature_pipeline.transform(company_data)
    probability = model.predict_proba(features)[0, 1]

    return {
        "bankruptcy_probability": probability,
        "risk_level": risk_level,
        "top_risk_factors": explanations[:5],
        "recommendations": actions
    }
```

## 📋 8단계: 최종 산출물

### 8.1 모델 파일
- `models/ensemble_model.pkl`
- `models/feature_pipeline.pkl`
- `models/scaler.pkl`

### 8.2 Streamlit 웹 애플리케이션
- 실시간 부도 위험 평가
- 대화형 시나리오 분석
- 설명 가능한 AI 예측
- 업계 벤치마킹

### 8.3 REST API 서비스
- 개별 기업 스코어링
- 배치 예측
- 모델 모니터링

### 8.4 문서화
- 모델 개발 보고서
- API 명세서
- 사용자 매뉴얼

## 🎯 성공 지표

### 모델 성능
- PR-AUC: 0.65 이상
- F2-Score: 0.70 이상
- Type II Error Rate: 20% 이하

### 비즈니스 가치
- 부도 조기 감지율: 75% 이상
- 오탐지율: 30% 이하
- 예상 손실 감소: 40% 이상

### 시스템 성능
- API 응답시간: < 500ms
- 동시 사용자: 100명 이상
- 가용성: 99.9%

## 📝 주요 특징

이 프로젝트의 핵심은 **도메인 지식 기반의 Feature Engineering**입니다:

1. **"왜 기업이 부도가 나는가?"**에 대한 깊은 이해
2. 한국 시장 특화 리스크 요인 반영
3. 재무조작 탐지 메커니즘
4. 이해관계자 행동 패턴 분석
5. 업종별 맞춤형 지표 개발

단순한 통계적 접근이 아닌, **비즈니스 통찰력**과 **도메인 전문성**을 결합한 예측 모델을 구축합니다.