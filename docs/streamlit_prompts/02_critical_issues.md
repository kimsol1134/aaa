# Part 2: 발견된 주요 차이점 분석

> **읽기 시간**: 10분 | **난이도**: ⭐⭐ 중급

이 문서는 노트북과 Streamlit 앱 간 발견된 8개 주요 차이점을 상세히 분석합니다.

---

## 발견된 이슈 요약

| # | 이슈 | 심각도 | 영향도 | 예상 작업 시간 |
|---|------|--------|--------|---------------|
| 1 | 모델 파일명 불일치 | 🔴 Critical | 시스템 작동 불가 | 10분 |
| 2 | Traffic Light 임계값 10배 차이 | 🔴 Critical | 잘못된 등급 판정 | 15분 |
| 3 | 특성 개수 불일치 (51 vs 65?) | 🔴 Critical | 차원 불일치 | 1시간 |
| 4 | SHAP 값이 가짜 (하드코딩) | 🟡 High | 신뢰성 문제 | 2시간 |
| 5 | 비즈니스 가치 분석 누락 | 🟡 High | ROI 미표시 | 2시간 |
| 6 | 데이터 유출 특성 미제거 | 🟡 High | 조기 경보 실패 | 30분 |
| 7 | 전처리 파이프라인 불일치 | 🟡 High | 예측 불안정 | 1시간 |
| 8 | 특성 선택 로직 없음 | 🟡 High | 다중공선성 | 1시간 |

---

## 🔴 Issue 1: 모델 파일명 불일치 ⭐⭐⭐⭐⭐

### 문제 설명
**노트북** (Part 3): CatBoost를 최종 모델로 선택 → `발표_Part3_v3_최종모델.pkl` 저장
**앱** (config.py): XGBoost 파일 로드 시도 → `best_model_XGBoost.pkl`

### 영향
- 앱 실행 시 모델 파일을 찾을 수 없음
- 휴리스틱 예측으로 폴백 → 정확도 낮음
- 사용자에게 잘못된 예측 제공

### 해결 방법
`config.py` 파일명 수정:
```python
MODEL_PATH = MODEL_DIR / '발표_Part3_v3_최종모델.pkl'  # CatBoost
THRESHOLDS_PATH = MODEL_DIR / '발표_Part3_v3_임계값.pkl'
```

---

## 🔴 Issue 2: Traffic Light 임계값 10배 차이 ⭐⭐⭐⭐⭐

### 문제 설명
**노트북** (Part 3): 통계적으로 계산된 최적 임계값
- Red: >= 0.0468 (Recall 80%)
- Yellow: >= 0.0168 (Recall 95%)
- Green: < 0.0168

**앱** (config.py): 임의 설정
- Warning: >= 0.6 (60%)
- Caution: >= 0.3 (30%)
- Safe: < 0.1 (10%)

### 영향
- 노트북 기준 High Risk(5%) 기업이 앱에서는 Safe로 분류됨
- Traffic Light 시스템이 완전히 다르게 작동
- 비즈니스 의사결정 오류

### 해결 방법
노트북 임계값으로 교체:
```python
TRAFFIC_LIGHT_THRESHOLDS = {
    'green': 0.0168,
    'yellow': 0.0468,
    'red': 1.0
}
```

---

## 🔴 Issue 3: 특성 개수 불일치 ⭐⭐⭐⭐

### 문제 설명
- **노트북**: 51개 특성 생성 (Part 2 실제 출력)
- **앱 주석**: 65개 특성 주장

### 원인 추정
- `composite_features.py`가 추가 특성 생성?
- 주석만 업데이트되고 실제 코드는 다름?
- 특성 카테고리별 개수가 미세하게 다름

### 해결 방법
1. 각 `src/domain_features/*.py` 파일 확인
2. 노트북 요약과 비교 (특성명, 공식)
3. 불일치 수정
4. 주석 업데이트

---

## 🟡 Issue 4: SHAP 값이 가짜 ⭐⭐⭐⭐

### 문제 설명
**노트북** (Part 4): 실제 SHAP 라이브러리 사용
```python
import shap
explainer = shap.TreeExplainer(model)
shap_values = explainer.shap_values(X_test)
```

**앱** (charts.py): 하드코딩된 규칙
```python
if feature_values.get('유동비율') < 1.0:
    important_features.append(('유동비율 부족', 0.05))  # 0.05는 어디서?
```

### 영향
- 모델의 실제 판단 근거를 보여주지 못함
- "SHAP"이라는 이름 사용하지만 기술적으로 거짓
- 사용자 신뢰도 하락

### 해결 방법
실제 SHAP 라이브러리로 재구현 (Task 4 참조)

---

## 🟡 Issue 5: 비즈니스 가치 분석 누락 ⭐⭐⭐

### 문제 설명
**노트북** (Part 4): 상세한 재무적 영향 분석
- TP/FP/FN/TN별 가치 계산
- ROI 920%, Payback 1.3개월
- Confusion Matrix 시각화

**앱**: 전혀 없음

### 영향
- 모델의 비즈니스 가치를 보여주지 못함
- 경영진 설득 불가능
- Part 4 핵심 인사이트 누락

### 해결 방법
새 모듈 `src/utils/business_value.py` 생성 (Task 5 참조)

---

## 🟡 Issue 6: 데이터 유출 특성 미제거 ⭐⭐⭐

### 문제 설명
**노트북** (Part 3): `이해관계자_불신지수` 제거
- 이유: 이미 부도난 기업만 신용등급 낮음
- 조기 경보 아님, 데이터 유출

**앱**: 제거 여부 불명확

### 해결 방법
`stakeholder_features.py`에서 해당 특성 제거 확인

---

## 🟡 Issue 7: 전처리 파이프라인 불일치 ⭐⭐⭐

### 문제 설명
**노트북** (Part 3): InfiniteHandler → Imputer → LogTransformer → RobustScaler
**앱**: replace(inf) → fillna(0)만

### 해결 방법
노트북과 동일한 파이프라인 구현

---

## 🟡 Issue 8: 특성 선택 로직 없음 ⭐⭐⭐

### 문제 설명
**노트북** (Part 2): IV > 0.02, VIF < 10 기준으로 특성 선택
**앱**: 모든 특성 사용

### 해결 방법
`feature_metadata_완전판.csv` 로드하여 선택된 특성만 사용

---

**다음 파일**: `03_task_details_priority1.md` (Task 1-3 상세 지시사항)
