# Part 3: 빠른 시작 가이드

> **목표**: DART API → 재무정보 수집 → 전처리 → 모델 예측 → 시각화

---

## 🎯 최종 목표

**사용자 플로우**:
```
기업명 입력 (예: "삼성전자")
    ↓
DART API로 재무제표 자동 수집
    ↓
노트북과 동일한 전처리 (51개 특성 생성)
    ↓
학습된 CatBoost 모델로 부도 확률 예측
    ↓
결과 시각화 (Traffic Light, SHAP, 비즈니스 가치)
```

---

## ⚡ Critical Tasks (반드시 먼저 수행)

### Task 1: 모델 파일 경로 수정 (10분)

**파일**: `streamlit_app/config.py`

**수정 전**:
```python
MODEL_PATH = MODEL_DIR / 'best_model_XGBoost.pkl'  # ❌
```

**수정 후**:
```python
MODEL_PATH = MODEL_DIR / '발표_Part3_v3_최종모델.pkl'  # ✅ CatBoost
THRESHOLDS_PATH = MODEL_DIR / '발표_Part3_v3_임계값.pkl'
```

**검증**:
```bash
ls -lh data/processed/발표_Part3_v3_*.pkl
# 파일이 존재하는지 확인
```

---

### Task 2: Traffic Light 임계값 수정 (15분)

**파일**: `streamlit_app/config.py` (line 37-43)

**수정 전**:
```python
RISK_THRESHOLDS = {
    'safe': 0.1,      # ❌ 10배 높음
    'caution': 0.3,
    'warning': 0.6,
}
```

**수정 후**:
```python
# Part 3 노트북의 최적 임계값 (Recall 80% 기준)
TRAFFIC_LIGHT_THRESHOLDS = {
    'green': 0.0168,   # < 1.68%: Safe
    'yellow': 0.0468,  # < 4.68%: Potential Risk
    'red': 1.0         # >= 4.68%: High Risk
}
```

**파일**: `src/utils/helpers.py` (`get_risk_level` 함수도 수정)

---

### Task 3: 특성 개수 확인 (30분)

**목표**: 노트북과 동일한 51개 특성 생성 확인

**방법**:
1. 노트북 요약 파일 읽기: `docs/notebook_summaries/발표_Part2_도메인_특성_공학_완전판_summary.md`
2. 각 카테고리별 특성 개수 확인:
   - 유동성: 9개
   - 지급불능: 11개
   - 재무조작: 17개
   - 이해관계자: 10개 (이해관계자_불신지수 제외!)
   - 한국시장: 6개
   - **합계: 51개** (composite 제외)

3. `src/domain_features/` 파일들 확인

---

## 🚀 앱 실행 및 테스트

### 1. 모델 파일 확인
```bash
ls -lh data/processed/
# 발표_Part3_v3_최종모델.pkl 있는지 확인
# 없으면 노트북 Part 3 실행 필요
```

### 2. 앱 실행
```bash
cd streamlit_app
streamlit run app.py
```

### 3. 테스트
- **샘플 데이터** 모드 선택
- "정상 기업" 선택
- 예측 결과 확인:
  - 부도 확률 < 1.68% → 🟢 Green
  - Traffic Light 등급 확인
  - SHAP 차트 확인

### 4. DART API 테스트
- "DART API 검색" 모드 선택
- 기업명: "삼성전자"
- 연도: 2023
- 조회 및 분석

---

## 📝 다음 단계

Priority 1 작업 완료 후:
1. `docs/streamlit_prompts/04_advanced_tasks.md` 참조 (SHAP, 비즈니스 가치)
2. 전체 검증 체크리스트 확인
3. 에러 케이스 테스트

---

**작성일**: 2025-11-23
